#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

DIR=$(cd "$(dirname "$0")" && pwd)

cd $DIR/../jupyterlite

python -m pip install -r requirements.txt
jupyter lite build --contents content --output-dir $DIR/../docs/html/_site/jupyterlite
